import { HiglightCompletedTodoDirective } from './higlight-completed-todo.directive';

describe('HiglightCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HiglightCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
